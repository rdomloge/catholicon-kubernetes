apiVersion: v1
kind: Service
metadata:
  name: catholicon-ms-seasons-service
spec:
  selector:
    app: catholicon-ms-seasons
  ports:
    - protocol: TCP
      port: 81
      targetPort: 8080
  type: LoadBalancer
  externalIPs:
  - 10.0.0.12
